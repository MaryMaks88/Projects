SMS Spam detection using Sequence to Vector RNN architecture.

Коли на вхід подається послідовність (наприклад, слова в реченні), 
а на виході отримуєють одне єдине число (ймовірність того, що це шахрайство).
Задача бінарної класифікації (0, 1).
